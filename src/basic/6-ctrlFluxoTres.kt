fun main() {
    var nome: String;
    var index: Int = 0;
    while (index < 100) {
        nome = "Emerson";
        println(nome);
        index++;
    }

    var l: Int = 10;
    do {
        println(l);
        l += 5;
    } while (l < 100);
}